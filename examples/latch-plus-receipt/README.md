# Latch + Receipt

Two gates for write tools:

1. **agent-latch** — sensitive args must be user/tool grounded  
2. **agent-receipt** — side effect must be verifiable in the world  

```bash
npm run example:receipt
```

No API keys required — simulates SMTP accept set.
